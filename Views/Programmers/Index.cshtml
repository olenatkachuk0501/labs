@model IEnumerable<Lab2_DIS.Models.Programmer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Programmer_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Main_Skill)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Programmer_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Main_Skill)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Programmer_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Programmer_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Programmer_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
